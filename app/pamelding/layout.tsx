import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Påmelding — Respawn Østfold',
  description: 'Meld deg på Respawn Østfold MCSR Ranked-turneringen. Påmelding stenger 9. juni 2026.',
};

export default function PameldingLayout({ children }: { children: React.ReactNode }) {
  return children;
}
